<div dir="rtl" align="right">

# ארכיטקטורה ומעקב פיצ'רים (Architecture & Feature Tracker)

קובץ זה מתעד את האסטרטגיה המערכתית והפיצ'רים שהתווספו לפרויקט Clinical AI.

## עקרונות אסטרטגיים (System Architecture Strategy)
1. **אוטומציה מלאה בהזנת נתונים (Auto-Ingestion):** התיקייה `docs_inbox` משמשת כתיקיית Drop. המערכת דוגמת אותה ברקע, מעבדת מסמכים באופן אוטומטי, ומעבירה אותם ל-`docs_archive` (תוך שמירה על מבנה תיקיות - התווסף ביולי 2026).
2. **הפרדה בין חילוץ לאישור (Human-in-the-Loop):** ה-LLM מציע מושגים, קשרים ודפי עבודה, אך הם נשמרים כטיוטות (Drafts) ולא מוזרקים ל-Neo4j עד שיש אישור ממשתמש אנושי דרך ממשק ה-Review App.
3. **עיבוד מבוסס הקשר ולוגיקה (Context-Aware Extraction):** המערכת מזהה אוטומטית אם מסמך הגיע מתיקייה בשם "דפי עבודה" (או מכיל תרגילים בכותרת) ומתייגת את החומר באופן מתאים, כאשר ברירת המחדל למשתמש באפליקציית האישורים מכוונת לסטטוס הנכון אוטומטית. חיתוך המסמכים נעשה באופן לוגי (לפי פסקאות וכותרות) ולא כחיתוך טקסט עיוור.
4. **סמכות מקור לפני סמכות תוכן:** כל חומר חדש נרשם לפי SHA-256 ומסווג כ־`METHOD_PRIMARY`, ‏`SECONDARY_INTERPRETIVE` או `UNVERIFIED`. תוכן או שם קובץ לבדם אינם הופכים חומר למקור רשמי.
5. **משוב דו־כיווני עם המילון:** מקורות רשמיים יכולים להציע ערך קנוני או שינוי; חומר משני יכול להציע רק מושג־משנה, דוגמה או קשר המעוגנים בערך קיים. כל המועמדים דורשים ביקורת מילון, ומהדורת מילון חתומה היא הדרך היחידה להחזיר ידע קנוני ל־Clinical AI.
6. **כשל בטוח:** חומר שסמכותו, מצב ה־PII או זכויות השימוש בו אינם תקינים עובר להסגר. אין קידום אוטומטי למילון או ל־Neo4j.
7. **תכנית אחת, שתי סביבות מוגנות:** Clinical AI ומילון דרך מנוהלים כתכנית אחת, אך אין Import ישיר או ערבוב קבצים ביניהם. המילון נשאר יצרן ידע קנוני מוגן, ו־Clinical AI צורך רק חבילות Preview או שחרור הקשורות למניפסט ולטביעות.

## פיצ'רים שהתווספו (Changelog / Feature Tracker)
- **תמיכה ב-PDF:** יכולת לעבד קבצי PDF באופן זהה לקבצי Word (יולי 2026).
- **תהליך אינג'סט אוטומטי:** סקריפט `auto_ingest_loop.py` שרץ ברקע ומזהה קבצים חדשים (יולי 2026).
- **ממשק אישורים משולב (Review App):** ממשק Streamlit יחיד שמאחד אישור מושגים, דפי עבודה, ויחסים (יולי 2026).
- **מיזוג אוטומטי (Auto-Merge):** זיהוי כפילויות מול מושגים שכבר אושרו (מונע אישור כפול של אותו מושג בדיוק).
- **חילוץ לפי הקשר תיקייה וחלוקה לוגית:** שמירת מבנה תיקיות מארכיון, וסיווג אוטומטי של תרגילים כשהמסמך יושב בתיקיית "דפי עבודה".
- **רישום וניתוב חומר חדש:** `material_intake_router.py` וסכמות `data/material_intake/` מפרידים בין תור קנוני, תור משלים והסגר, ללא כתיבה לגרף.

</div>
