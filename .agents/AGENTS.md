# AGENTS RULES - CLINICAL AI

## Global Rules
> This project inherits ALL global rules from:
> `C:\Avihusitton\_Global_Antigravity\GEMINI.md`
> Read it FIRST.

## Local Overrides
- **CHANGELOG Rule (Mandatory for ALL AI agents including Gemini & ChatGPT)**: After completing *any* task, you must evaluate if the change was significant (e.g. new features, architectural changes, major UI updates, data model changes). If it is significant, you MUST append a brief, concise entry to `CHANGELOG.md` in the project root. Format: `[YYYY-MM-DD HH:MM] - <Concise summary> - <Agent (e.g. Gemini 3.1 Pro / ChatGPT o1)>`. This rule ensures sync across different models.
