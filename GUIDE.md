# מדריך הפעלה — המוח המערכתי

מדריך זה כתוב בשבילך כמנהל פרויקט, לא כמתכנת. כל שלב הוא פקודה להעתיק-להדביק
בטרמינל (או הנחיה לסוכן הקוד - Cline - להריץ בשבילך).

## מה זה בכלל

מערכת שקוראת ~400 עמודי Word של סיכומי שיעורים, מבינה מה שייך למה (איזה
מושג, איזה תרגיל, איזה שיעור, איזה הקשר טיפולי), ובונה מזה גרף ידע ב-Neo4j.
בסוף, אפשר לשאול אותה שאלה קלינית והיא תשלוף תשובה מבוססת-ציטוטים, לא
ניחוש.

**חשוב להבין:** זו לא "תריץ קובץ אחד וזהו". זו עבודה של כמה ימים, עם
בדיקות ביניים - בדיוק כמו שפייבל תכנן. אל תדלגו על שלבי הבדיקה.

---

## שלב 0: הכנה (30 דקות)

1. ודאו שמותקן Python 3.11+ ו-Neo4j (עם **תוסף APOC מותקן** - זה לא
   ברירת מחדל, צריך להפעיל אותו ידנית בהתקנת Neo4j. בלעדיו הקוד ייכשל
   בשלב הטעינה לגרף עם שגיאה לא ברורה).
2. פתחו טרמינל בתיקיית הפרויקט והריצו:
   ```
   pip install -r requirements.txt
   python setup.py
   ```
   זה יוצר את כל התיקיות (`docs_inbox`, `docs_archive`, `docs_error`,
   `data`, `out`) וקבצי דוגמה.
3. פתחו את `data/glossary.json` ו-`data/exercises.json`. יש שם 2 מושגים
   לדוגמה בכל קובץ - **אלה רק דוגמאות להראות את המבנה**. תצטרכו למלא
   בפועל את כ-300 המושגים וה-30-50 התרגילים האמיתיים של השיטה, כל אחד עם
   שם קנוני, מילים נרדפות, והגדרה קצרה.
   - זו כנראה העבודה הכי ארוכה בכל הפרויקט, ולא ניתן לעשות אותה בשבילכם
     כי זה התוכן הקליני של השיטה. אם תרצו עזרה בניסוח ההגדרות מתוך חומרי
     הקורס - זו משימה נפרדת שאפשר לעבוד עליה יחד.
4. פתחו את `.env` (נוצר אוטומטית מ-`.env.example`) ומלאו: מפתח
   OpenRouter, ופרטי החיבור ל-Neo4j.

## שלב 1: בדיקה בלי לגעת בכלום אמיתי (10 דקות)

לפני שמכניסים קובץ אמיתי אחד, מריצים על נתונים מזויפים כדי לוודא שהכל
מותקן נכון:
```
python ingestion_pipeline.py --dry-run --mock-llm --limit 1
```
`--mock-llm` = כל תשובות ה-AI מזויפות (לא באמת מנקה פרטים מזהים!),
`--dry-run` = לא נוגע ב-Neo4j. זה בודק רק שהקוד רץ בלי שגיאות.
**אף פעם אל תשתמשו ב-`--mock-llm` על קבצים אמיתיים של מטופלים** - שימו
קובץ בדיקה מומצא בלבד לצורך זה.

## שלב 2: קובץ אמיתי ראשון (יום)

הסירו את `--mock-llm`. שימו קובץ Word **אחד** אמיתי בתוך `docs_inbox`,
ואז:
```
python ingestion_pipeline.py --dry-run --limit 1
```
עכשיו זה באמת קורא ל-OpenRouter (אנונימיזציה אמיתית, סיווג הקשר טיפולי
אמיתי, אימות מושגים אמיתי) אבל עדיין לא נוגע ב-Neo4j. **בדקו ידנית**:
- `out/anchors_report.json` - האם מספרי השיעורים שזוהו נכונים?
- `out/linking_stats.json` - כמה אחוז מה-chunks קיבלו מועמד מושג
  (`candidate_coverage_pct`)? אם זה מתחת ל-30%, כנראה שהגלוסר עדיין דל
  מדי (טבעי בהתחלה, לפני שמילאתם את כל 300 המושגים).
- הקובץ עצמו - עבר לתיקיית `docs_archive` עם תאריך בשם? מעולה, זה
  אומר שהעיבוד הצליח.

## שלב 3: מדגם של 5 קבצים (יום)

```
python ingestion_pipeline.py --dry-run --limit 5
```
בדקו שוב את `out/anchors_report.json` - הפעם על מדגם שכולל, אם אפשר,
קובץ עם "יומן מתגלגל" (כמה שיעורים באותו קובץ), קובץ בלי תאריך בכלל,
וקובץ עם שגיאות כתיב. רק אחרי שהמדגם הזה נראה נקי, ממשיכים הלאה.

## שלב 4: כל הקורפוס, עדיין בלי Neo4j (יומיים)

```
python ingestion_pipeline.py --dry-run
```
(בלי `--limit` = כל הקבצים ב-`docs_inbox`). בדקו:
- `out/manifest.json` - כמה קבצים נכשלו (`failed_read` / `failed_deid`
  / `duplicate`)? קבצים כאלה יעברו אוטומטית לתיקיית `docs_error` - אם
  היא לא ריקה, זה בדיוק המקום לבדוק מה קרה.
- `out/waiting_room.json` - מושגים ש-AI לא היה בטוח לגביהם, מקובצים
  יחד. ככל שתעשירו את הגלוסר עם מילים נרדפות מהמקרים האלה, הרשימה הזו
  אמורה להצטמצם עם הזמן.
- `out/conflicts_report.json` - סתירות בין קשרים תיאורטיים שהתגלו (למשל
  שיעור 2 טוען דבר אחד ושיעור 30 טוען ההפך). אלה דורשים החלטה אנושית -
  האם זו התפתחות של השיטה, הקשרים קליניים שונים, או טעות.

## שלב 5: טעינה אמיתית ל-Neo4j (יום)

רק אחרי ששלב 4 נראה נקי:
```
python ingestion_pipeline.py
```
בלי `--dry-run` = הפעם זה גם כותב לגרף. אפשר להריץ את זה שוב בבטחה גם
בעתיד (למשל כשיתווספו קבצים חדשים) - הקוד לא יוצר כפילויות בגרף.

## שלב 6: שאילתה ראשונה

```
python retrieval.py --question "מטופל אוכל גלידה בלילות אחרי ריב עם אמו" --modality individual
```
זה מחפש את המושג הכי קרוב לשאלה, עושה חשיבה רב-שלבית בגרף (עד 2 קפיצות,
רק דרך קשרים תיאורטיים - לא דרך תרגילים), ומרכיב תשובה עם שרשרת נימוקים
מצוטטת והערת אי-התאמה אם ההקשר הטיפולי של הראיות שונה מהמקרה שלכם.

---

## מה בכל קובץ (בקצרה)

| קובץ | מה הוא עושה |
|---|---|
| `setup.py` | מריצים פעם אחת בהתחלה - בונה תיקיות וקבצי תבנית |
| `config.py` | כל המספרים/נתיבים המכווננים, במקום אחד |
| `llm_client.py` | כל קריאה ל-AI (OpenRouter) עוברת דרך כאן |
| `file_manager.py` | ארכיטקטורת 3 התיקיות (Inbox/Archive/Error) |
| `ingestion_pipeline.py` | **הקובץ המרכזי** - כל שלבי העיבוד |
| `retrieval.py` | שאילתות ושליפת תשובות מהגרף המוגמר |
| `data/glossary.json` | **אתם ממלאים** - 300 המושגים האמיתיים |
| `data/exercises.json` | **אתם ממלאים** - 30-50 התרגילים האמיתיים |
| `data/relationship_types.json` | **אתם בודקים/משלימים** - טיפוסי הקשר וטבלת הסתירות |
| `run_all.py` | פקודה אחת שמריצה הכל (התקנה + setup + dry-run) |
| `diagnose_docx.py` | אבחון קובץ בודד לפני עיבוד - ראו למטה |

## חדש: קבצים "רב-סוגיים" ומסמכי שיטה רשמיים

מהענף המקביל התברר שני דברים חשובים על הקורפוס האמיתי:

1. **תאריכים לפעמים חיים בכותרת העמוד (header), לא בגוף הטקסט** - ותוכן
   הצינור המקורי היה עיוור לזה לגמרי. עכשיו `DocxReader` קורא גם כותרות
   עליונות, ומזריק אותן בדיוק בגבול הסעיף שבו הן משתנות.
2. **חלק מהקבצים הם מסמכי שיטה רשמיים** (עם לוגו/כותרת קבועה, כותרת
   תחתונה עם טלפון/מייל, והרבה כותרות מבניות) - לא תמלילי שיעור. אלה
   מזוהים אוטומטית (`DocumentTypeClassifier`) ומחולקים לפי מבנה הכותרות
   שלהם (`chunk_by_headings`) במקום לפי ספירת תווים, ומסומנים TIMELESS
   (ידע כללי של השיטה, לא קשור לתאריך שיעור ספציפי).

**כלי חדש - `diagnose_docx.py`**: לפני שמעלים קובץ חדש שנראה חריג, אפשר
להריץ:
```
python diagnose_docx.py docs_inbox/הקובץ.docx
```
זה מראה כמה סעיפים יש, מה יש בכותרות עליונות/תחתונות, ואם יש תיבות טקסט
(textboxes) - תוכן שהצינור לא קורא בכלל וכדאי לדעת על זה. עם `--search
"טקסט"` אפשר גם לאתר בדיוק איפה טקסט מסוים חי בקובץ.

זה נבדק בפועל על שני קבצי בדיקה סינתטיים: תמליל דו-סעיפי עם תאריך שחי
רק בכותרת של הסעיף השני (זוהה נכון כעוגן שיעור 7), ומסמך שיטה עם לוגו,
כותרת תחתונה עם פרטי קשר, ושתי רמות כותרות (סווג נכון כ-OFFICIAL_METHOD_DOC
וחולק נכון ל-4 chunks לפי heading_path, כולם TIMELESS).

## שינויים והשלמות שעשיתי לעומת מה שפייבל/ג'מיני כתבו בפועל

כדי שתדעו בדיוק מה חדש ומה כבר היה קיים:

1. **De-identification הפך משלד לקוד אמיתי**, מחובר ל-OpenRouter
   (`deepseek/deepseek-v4-flash` - מודל זול, אפשר להחליף ב-.env). שיניתי
   התנהגות: בלי מפתח API, הקובץ **נכשל** במפורש במקום "לדמות" ניקוי -
   כדי שאי אפשר יהיה בטעות להריץ דאטה אמיתי בלי אנונימיזציה אמיתית.
2. **שלב האימות הבינארי (yes/no/unclear)** - פייבל תיאר את זה בפירוט אבל
   כתב "זה שלב נפרד, מחוץ לקובץ הזה". השלמתי אותו (`CandidateVerifier`).
   זה דרש גם להוסיף שדה `definition` לכל מושג בגלוסר - זה לא היה בפורמט
   המקורי, אבל בלי הגדרה אי אפשר לשאול את המודל "האם זה מתאים למושג הזה".
3. **קשרים תיאורטיים בין מושגים** (is_symptom_of וכו') - זה בכלל לא היה
   מפורט בשום שיחה שקיבלתי. השלמתי את זה לפי אותה פילוסופיה שפייבל כבר
   קבע (סט סגור + LLM שופט + ברירת מחדל לשתיקה) - אבל זה החלק הכי "חדש",
   אז כדאי לעבור על התוצאות הראשונות שלו בעין הכי ביקורתית.
4. **שכבת ה-retrieval** - פייבל שאל "רוצה שאמשיך לשם?" ולא קיבל תשובה
   בשיחות שקיבלתי, אז זה מעולם לא נכתב. בניתי אותה לפי המפרט המדויק
   שהוא כן נתן (whitelist מפורש, Exercise לא בטראברסל ההיסקי, שרשרת
   hop-אחר-hop, איסור שפת המלצה ישירה).
5. **"דלת הכניסה" בלי Pinecone** - הלכתי על ההצעה הפשוטה יותר של פייבל
   (התאמה מורפולוגית/fuzzy, בלי embeddings נפרדים בכלל) כי הגלוסר קטן
   מספיק (300-350 פריטים) שזה מספיק טוב, וזה חוסך תלות שלמה (מודל
   embeddings + אינדקס וקטורי). אם זה יתברר כלא מדויק מספיק בפועל -
   שדרוג לווקטורים הוא שינוי מוכל בפונקציה אחת.
6. **דה-דופליקציה בין ריצות נפרדות** - בדקתי את זה בפועל ומצאתי שהגרסה
   שכתבתי בהתחלה זיהתה כפילויות רק בתוך אותה ריצה, לא מול קבצים שכבר
   טופלו בעבר. תיקנתי עם קובץ רישום (`data/content_hashes.json`) שנשמר
   בין ריצות - זו בדיוק הבעיה האמיתית של קבצי הוואטסאפ שכל הארכיטקטורה
   נבנתה כדי לפתור, אז חשוב שזה יעבוד נכון.
7. **ה-Chunk (לא הפסקה) הוא יחידת הסיווג** לסיווג הקשר הטיפולי - ג'מיני
   הציע ברמת הפסקה, אבל זה קריאת LLM נפרדת לכל פסקה. chunk הוא כבר
   ~1-2 פסקאות, אז זה חוסך משמעותית בעלות בלי לוותר על הדיוק כמעט בכלל.

כל הקוד נבדק בפועל: הרצתי אותו על קובץ Word סינתטי עם 3 כותרות שיעור
(אחת עם שגיאת כתיב), תאריך נרטיבי בתוך משפט, ודילוג במספור - כל המנגנונים
עבדו כמצופה (הכותרות זוהו, התאריך הנרטיבי לא הרעיל את המצב, האזהרות
נכתבו). את קריאות ה-OpenRouter וה-Neo4j האמיתיות לא יכולתי לבדוק כאן
(אין לי גישה לאינטרנט הפתוח או לשרת Neo4j בסביבה הזו) - אלה ייבדקו
אצלכם בשלבים 2-5 למעלה.

## חדש: git, כדי שזה לא יקרה שוב

מה שגילינו בסבב הזה (קובץ שעובד פעמיים כי הועתק בטעות מהארכיון בחזרה
לאינבוקס, בלי היסטוריה שמראה מה קרה) קרה גם פעם עם Goose/Antigravity.
שני הענפים שהתפתחו בלי מעקב הם הסימפטום, לא הבעיה - הבעיה היא שאין
תיעוד של "מה השתנה ומתי". git פותר את זה.

**הקמה חד-פעמית** (בתיקיית הפרויקט):
```
git init
git add .
git commit -m "מצב התחלתי אחרי מיזוג"
```
מעכשיו, כל שינוי משמעותי (שלכם או שלי) הוא commit, לא קובץ `.bak` חדש:
```
git add -A
git commit -m "תיאור קצר של מה השתנה"
```
ואם משהו נשבר, `git diff` מראה בדיוק מה השתנה, ו-`git checkout -- קובץ`
מחזיר קובץ בודד למצב הקודם - בלי לחפש ידנית בין `.bak`, `.bak2` וקבצי
checkpoint.

## תיקון קריטי בסבב הזה: chunk_id כבר לא תלוי בשם הקובץ

התגלה קובץ ("ירוחם") שעובד פעמיים כי הועתק בטעות בחזרה מ-`docs_archive`
ל-`docs_inbox`. ה-`chunk_id` היה מבוסס על שם הקובץ, אז כפילות בפועל
יצרה chunks עם chunk_id שונה בגרף - ה-MERGE לא תפס את זה. עכשיו chunk_id
מבוסס על hash של התוכן עצמו, לא על השם - אז גם אם קובץ יעובד פעמיים
בטעות, הגרף לא ייפגע. בנוסף, קובץ ששמו נראה כמו קובץ מהארכיון (`_processed_תאריך`)
מקבל אזהרה מפורשת בלוג.

**`neo4j_diagnose_duplicates.py`** (חדש, קריאה בלבד) - בודק אם הבאג הזה
כבר השאיר עותקים כפולים בגרף האמיתי שלכם:
```
python neo4j_diagnose_duplicates.py
```

## תיקיית `legacy_scripts/`

סקריפטים חד-פעמיים שכבר מילאו את תפקידם (תיקוני קוד ידניים, גיבויים) -
נשמרו כאן לצורך תיעוד בלבד. אחרי שה-git יוקם, אין צורך ליצור סקריפט
חדש לכל שינוי - זה בדיוק מה ש-commit עושה.

**כלים אמיתיים שנשארו פעילים** (לא בתיקיית legacy): `build_glossary.py`
→ `curate_glossary.py` → `refine_drafts.py` → `review_glossary.py` הם
שרשרת עבודה אמיתית לניסוח טיוטת גלוסר מתוך קבצים שכבר עובדו, ולא סקריפט
חד-פעמי. `test_candidate_matching.py` ו-`test_live_llm.py` הם כלי בדיקה
שימושיים לחזור אליהם.

**תשומת לב:** `document_types_inspector.py` הועבר ל-legacy כי הוא
מצביע על `data/document_types.json` שלא קיים בפועל - כנראה כיוון שננטש
באמצע. אם היה לזה תפקיד מתוכנן, תזכיר לי ונשלים אותו.

- **ממשק אנושי לחדר ההמתנה** - כרגע זה קובץ JSON מקובץ, לא "טינדר של
  מושגים" עם כפתורים. פונקציונלי לגמרי, פחות נוח. אם זה יהפוך לצוואר
  בקבוק אמיתי - אפשר לבנות ממשק קטן בנפרד.
- **פתרון סתירות אוטומטי** - קונפליקטים נכתבים לדוח, אבל ההחלטה
  (התפתחות/הקשרים שונים/טעות) עדיין ידנית.
